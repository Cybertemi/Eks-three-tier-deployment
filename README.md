### Three-Tier Application Deployment on Kubernetes (EKS)

📌 Project Overview

This project demonstrates the end-to-end deployment of a production-style three-tier web application on Amazon Elastic Kubernetes Service, following modern DevOps and cloud-native best practices.

The application is fully containerized using Docker and orchestrated with Kubernetes. Infrastructure provisioning is automated using eksctl, while monitoring and observability are implemented using Prometheus and Grafana which were deployed via Helm.



### Architecture

Users (Browser)


      ↓
LoadBalancer Service


      ↓
Frontend (Nginx)


      ↓
Backend API (Node.js)


      ↓
MongoDB Database


### Prerequisites

Docker – Containerization

Kubernetes – Orchestration

Amazon Elastic Kubernetes Service – Managed cluster

eksctl – Cluster provisioning

kubectl – Cluster management

Helm – Package deployment

Prometheus – Metrics collection

Grafana – Visualization of metrics

### Deployment Steps


1️⃣ Create EKS Cluster

eksctl create cluster \
  --name three-tier-app-eks \
  --region us-east-1 \
  --nodegroup-name three-tier-app-nodes \
  --node-type t3.medium \
  --nodes 2 \
  --managed

2️⃣ Deploy Database (mongodb)

kubectl apply -f mongo-deployment.yaml
kubectl apply -f mongo-service.yaml

3️⃣ Deploy Backend

kubectl apply -f configmap.yaml
kubectl apply -f secret.yaml
kubectl apply -f backend-deployment.yaml
kubectl apply -f backend-service.yaml

4️⃣ Deploy Frontend

kubectl apply -f frontend-deployment.yaml
kubectl apply -f frontend-service.yaml


Expose using:

kubectl get svc -n workshop


Open the LoadBalancer EXTERNAL-IP/DNS in your browser.

5️⃣ Install Monitoring Stack & create namespace

helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

kubectl create namespace monitoring

helm install monitoring prometheus-community/kube-prometheus-stack \
  --namespace monitoring


For Access, run:

kubectl port-forward svc/monitoring-grafana 3000:80 -n monitoring


On your browser, open:

http://localhost:3000

### 📊 Monitoring Features

### Prometheus

Prometheus automatically collects, stores, and manages real-time metrics from your Kubernetes cluster and applications, including:

✅Pod performance metrics

✅CPU and memory usage

✅Node health and resource utilization

✅Service and application-level statistics

### Grafana

Grafana visualizes these metrics generated by the Prometheus through interactive dashboards, enabling:

✅Prebuilt and customizable dashboards

✅Real-time monitoring

✅Easier troubleshooting and alerting

✅Visualization and analysis of cluster performance


### Conclusion

This project simulates a real-world DevOps workflow where an application is built, deployed, exposed to the internet, monitored, and maintained in the cloud. Through this project, I gained hands-on experience deploying cloud-native applications, troubleshooting pod and network issues, exposing services securely and applying DevOps best practices for scalable, observable, and maintainable systems.

👤 Author

Temitope Ilori
linkedin: http://www.linkedin.com/in/iloritemi
